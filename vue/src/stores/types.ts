interface Option {
    id: number,
    name: string,
    link?: number
}

interface Result {
    city?: string,
    workshop?: string,
    employee?: string,
    brigade?: string,
    workshift?: string
}